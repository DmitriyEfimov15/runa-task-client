services:
  web:
    image: ${REGISTRY_URL}/${REGISTRY_REPO}:${VERSION}
    env_file: [.env]
    environment:
      - NODE_ENV=production
      - PORT=3000
    expose:
      - "3000"
    networks:
      - edge

  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - web
    networks:
      - edge

networks:
  edge:
    external: true
