import { LoginCard } from "@/src/features/auth/ui/login-card";
import CenterFlex from "@/src/shared/ui/centerFlex";

const Login = () => {
  return (
    <CenterFlex>
      <LoginCard />
    </CenterFlex>
  );
};

export default Login;
